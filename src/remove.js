export let removeItem = () => {
	// removeFromCart.onclick = () => {
	// 	let index = itemList.findIndex((x) => x.name === name.textContent);
	// 	itemList.splice(index, 1);
	// 	//clear and refresh product listing
	// 	inventoryList.innerHTML = '';
	// 	displayInventory();
	// };
};
